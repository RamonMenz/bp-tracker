# Variáveis de ambiente locais
.env.local
.env.*.local

# Credenciais do Firebase — nunca versionar
google-services.json
GoogleService-Info.plist
serviceAccountKey.json

# Dependências
node_modules/
functions/node_modules/

# Builds e cache
dist/
web-build/
.expo/
functions/lib/
*.tsbuildinfo

# Logs do emulador do Firebase (gerados a cada `emulators:start`/`emulators:exec`)
firebase-debug.log
firestore-debug.log
ui-debug.log

# Credenciais do EAS
credentials.json
